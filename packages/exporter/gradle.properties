org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# ── 版本（已于 2026-06-13 核对 maven 元数据）────────────────────────
# Minecraft 1.21.1 / NeoForge 21.1.x / Java 21
# neo_version is the compile target; neo_min_version is the runtime dependency floor.
minecraft_version=1.21.1
neo_version=21.1.233
neo_min_version=21.1.1
moddev_version=2.0.141
sqlite_jdbc_version=3.53.2.0

# ── Mod 元信息 ──────────────────────────────────────────────────
mod_id=mpide_exporter
mod_name=ModPack IDE Exporter
mod_group=io.github.aeroseira.mpide_exporter
mod_version=0.1.0
mod_license=GPL-3.0-only
mod_authors=Aeroseira
# 注意：gradle.properties 按 ISO-8859-1 读取，勿放非 ASCII（中文会乱码）。
mod_description=In-game final-state data exporter for ModPack IDE (offline consumption). NeoForge 1.21.1.
